import { ReservationSearchPipe } from './reservation-search.pipe';

describe('ReservationSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ReservationSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
